# Задача: перенос qd2 с Windows Forms в браузер

## Статус

**Этап 0 завершён** (влит в `master` коммитом `95161fd`, 2026-08-23). Из шести
исходных пунктов: три сделаны, один сделан за 22 партии (вынос диалогов,
отдельный отчёт — `docs/tasks/web-migration-dialogs.md`), один перенесён в
этап 4, один вычеркнут как ненужный. Подробности — в разделе 6, этап 0.

**Технология выбрана: ASP.NET Core 10 + Blazor Server** (решение принято
2026-08-23, обоснование — раздел 5.1 и 5.5). Открытый вопрос №1 закрыт.

**Вертикальный срез сделан** (2026-08-24, ветка `feature/web-slice`). Замер и
находки — в разделе 10. Короткий итог: экран стоит **ноль строк кода** —
журнал работает для любой сущности по метаданным; 34 из 56 активных сущностей
с паспортом покрыты уже сейчас. Дальше — этап 1.

Остальная часть документа — аналитическая, написана до начала работ: замеры
кодовой базы (август 2026), классификация кода по стоимости переноса, выбор
технологии интерфейса, оценка объёма.

Исходные требования (сформулированы устно):

1. Минимальный объём изменений. Не улучшать то, что работает, только потому что
   «делаем новое».
2. Бизнес-логика остаётся в хранимых процедурах. Процедуро-центричная
   архитектура сохраняется как есть.
3. Существующие классы, выполняющие свою функцию, переиспользуются.
4. Изменения касаются только слоя пользовательского интерфейса: вместо
   Windows Forms — браузер.
5. Технология визуализации выбирается по критерию простоты: простота написания
   кода, простота поддержки, простота понимания.

## 1. Замеры текущей системы

| Показатель | Значение |
|---|---|
| C# без `*.Designer.cs` | `Client` 42 479 строк, `FogSoft.WinForm` 18 569 |
| `*.Designer.cs` (чистая раскладка WinForms) | 15 103 строки — выбрасывается целиком |
| Форм / контролов / доменных классов | 69 / 19 / 111 |
| Хранимых процедур в `ArtvisDB` | 381, из них 271 зарегистрирована в `iStoredProcedure` |
| Таблиц / функций / представлений | 102 / 63 / 4 |
| Сущностей `iEntity` | 164 (144 активных); 69 с XML-паспортом, 57 с XML-фильтром |
| Пунктов меню `iMenu` | 110 (89 активных) |
| Crystal-отчётов | 14 `.rpt` + обвязка в `Client/Reports` (39 файлов) |
| Целевой фреймворк | .NET Framework 4.8 |

Замеры сущностей и меню сняты на dev-базе (`.\sqlexpress` / `ArtvisDev`).

## 2. Ключевой вывод: большая часть UI — это движок и метаданные

Значительная доля экранов не написана руками:

- **Меню и права** грузятся из БД:
  `ArtvisDB/dbo/Stored Procedures/UserMenuItems.sql` (`iMenu` + `GroupMenu` +
  `UserAdditionMenu`), рендерится в `Client/Forms/MDIForm.cs`.
- **Журналы** строятся универсально: `Globals.ShowSimpleJournal(Entity, ...)`
  → `JournalForm`. В `MDIForm.cs` — 36 таких вызовов.
- **Карточки сущностей (паспорта)** собираются в рантайме из XML в колонке
  `iEntity.passport` движком `FogSoft.WinForm/Passport/Classes/*`
  (словарь контролов — `PageControl.Attributes`: `field`, `lookup`,
  `objectPicker`, `selector`, `treeselector`, `image`, `button`, `label`,
  `separator`). Подробности — `docs/ARCHITECTURE.md`, раздел
  «Metadata-driven passport forms».
- **Фильтры** — XML в `iEntity.filter` (57 сущностей).

Следствие для плана: один веб-рендерер журнала, фильтра и паспорта закрывает
большинство из 89 активных пунктов меню **без кода на каждый экран**. И после
миграции добавление сущности останется, как сейчас, чисто метаданными (колонка
в таблице и view, параметр в IUD-процедуре, строка в `iTableAlias`, XML в
`iEntity.passport`). Это точка максимального рычага, вокруг которой строится
весь план.

## 3. Где реально проходит граница «UI / не-UI»

Требование «менять только UI» в буквальном виде невыполнимо: граница в коде не
проведена. Факты по `Client/Classes` (85 файлов):

- 54 файла ссылаются на `System.Windows.Forms` / `System.Drawing`;
- **56 вызовов `ShowDialog` и 55 обращений к `DialogResult` находятся внутри
  доменных классов**: `Action.cs`, `ActionOnMassmedia.cs`, `Campaign.cs`,
  `Agency.cs`, `ActionRoller.cs`, `ActionRollerInStatJournal.cs` и др.
  Пример: `Client/Classes/Action.cs:383` — доменный метод сам открывает
  `FrmDateSelector`, блокируется и читает `DialogResult`;
- 52 обращения к `Cursor.` и 51 `Application.DoEvents()` — шум, удаляется
  механически;
- базовый класс всех сущностей `FogSoft.WinForm/Classes/PresentationObject.cs`
  импортирует `System.Windows.Forms` и содержит `ShowPassport(IWin32Window)`;
- `FogSoft.WinForm/Globals.cs` — статический класс, наполовину UI
  (`MdiParent`, `ShowSimpleJournal`, `SetWaitCursor`, `CopyToClipboard`,
  `ShowMessageError`), наполовину полезные утилиты
  (`ResolveFilterInitialValues`, `SetParameterValue`, `CreateIDs`).

Это **не** повод переписывать бизнес-логику: расчёты и последовательность
вызовов процедур сохраняются один в один. Но каждый из ~56 сайтов с диалогом
придётся разрезать на «подготовить варианты» и «выполнить с выбором
пользователя». Работа делается **в существующем десктопе**, не ломая его
(этап 0).

### 3.1. Глобальное состояние

В десктопе на процесс приходится один пользователь, поэтому статика безопасна.
В вебе она становится общей на всех:

| Место | Сейчас | Что делать |
|---|---|---|
| `FogSoft.WinForm/Classes/SecurityManager.cs:97` | `private static User loggedUser` | хранилище → `AsyncLocal`, публичный API без изменений |
| `FogSoft.WinForm/DataAccess/DataAccessor.cs:88` | `[ThreadStatic] private static SqlTransaction _transaction` | `AsyncLocal` (`[ThreadStatic]` ломается на первом `await`) |
| `FogSoft.WinForm/DataAccess/DataAccessor.cs:89` | `[ThreadStatic] SqlParameter[] _commandParameters` | то же |
| `EntityManager` (кэш сущностей), `procedureConfigs`, `IMemoryCache` | статические кэши | оставить как есть — read-only, общие для всех, это корректно |

Замена хранилища при сохранении сигнатур означает: **ни одно из 100+ мест
вызова не меняется**. Это главный пример «минимального изменения с
максимальным эффектом».

### 3.2. Транзакции — хорошая новость

Все явные `BeginTransaction` — 10 сайтов, все в UI-слое:
`Client/Forms/ActionForm.cs`, `CampaignForm.cs`,
`CreateActionMaster/ComboModulePlacementForm.cs`, `EditIssuesForm.cs`,
`Client/Controls/PackModuleGrid.cs`, `ProgramIssuesGrid2.cs`,
`RollerIssuesGrid3.cs`.

Проверено: begin/commit находятся внутри одного обработчика, **ни одна
транзакция не оборачивает пользовательский диалог**. Значит, каждая транзакция
целиком укладывается в один HTTP-запрос, переделывать границы не нужно.

### 3.3. Подключение к БД

Строка подключения — один SQL-логин `AdvertAgUser` на всё приложение, а
идентификация пользователя передаётся в процедуры параметром `loggedUserId`
(`DataAccessor`). Для веба это идеальная схема: не нужны ни персональные логины
в СУБД, ни impersonation, пул соединений работает штатно. Менять нечего.

## 4. Классификация кода по стоимости переноса

### 4.1. Переносится практически бесплатно

- `FogSoft.WinForm/DataAccess/*` — `DataAccessor`, `DbExecutionScope`,
  `TvpValue`, `SqlBulkCopyHelper`, `MessageAccessor`;
- `Microsoft.ApplicationBlocks.Data/SqlHelper` (2775 строк);
- метаданные: `Entity`, `EntityManager`, `ColumnInfo`, резолвинг процедур через
  `ProcedureConfigurationRetrieve`;
- `SecurityManager`, логика прав из `MenuManager` (без `ToolStrip`),
  `ConfigurationUtil`, `StringUtil`;
- **все 381 хранимая процедура — ноль изменений**;
- ~31 файл `Client/Classes` без ссылок на UI плюс параметрическая часть
  остальных.

### 4.2. Переписывается по существующему образцу (движки)

- рендерер паспорта: диспетчеризация по имени XML-элемента переносится из
  `PageControl.CreateInstance` практически один в один; путь сохранения
  (`ApplyChanges` → словарь параметров → IUD-процедура) сохраняется дословно;
- журнал (список, сортировка, постраничность, панель инструментов);
- форма фильтра из `iEntity.filter`;
- селекторы (`SelectionForm`, `ObjectsSelector`, `TreeObjectsSelector`),
  `MasterDetailForm`.

### 4.3. Переписывается по-настоящему (~70% трудозатрат)

Экраны с owner-drawn сетками, кликом по ячейке, мультивыбором и пересчётом:

| Файл | Строк |
|---|---|
| `Client/Controls/PriceCalculatorGrid.cs` | 2095 |
| `Client/Forms/CampaignForm.cs` | 1616 |
| `Client/Forms/MDIForm.cs` (оболочка) | 1161 |
| `Client/Forms/PriceCalculatorForm.cs` | 1002 |
| `Client/Controls/TrafficGrid.cs` | 807 |
| `Client/Controls/TariffGrid.cs` | 759 |
| `Client/Forms/FrmTemplate3.cs` | 754 |
| `Client/Controls/TariffWithRangeGrid.cs` | 740 |
| `Client/Forms/FrmGenerator.cs` | 655 |
| `Client/Controls/ComboModuleGrid.cs` | 506 |
| `Client/Forms/CreateActionMaster/ComboModulePlacementForm.cs` | 496 |
| `Client/Controls/RollerIssuesGrid3.cs` | 478 |
| `Client/Controls/TariffWindowGrid.cs` | 465 |
| `Client/Forms/CreateActionMaster/EditIssuesForm.cs` | 409 |
| `Client/Forms/GridReport/*` | ~550 |

Перед переносом этих экранов обязательно читать соответствующие карты
сценариев в `docs/scenarios/` — они описывают путь от клика до
`IssueIUD` / `ActionRecalculate` и обратно.

### 4.4. Враждебно вебу — требует отдельного решения

| Что | Проблема | Минимальное решение |
|---|---|---|
| Crystal Reports, 14 `.rpt` (`Client/Reports`) | рантайм только под .NET Framework | вынести в отдельный процесс-сервис на 4.8: на входе имя отчёта и параметры, на выходе PDF-поток. Сами `.rpt` и обвязка не трогаются |
| Excel через COM Interop: `FogSoft.WinForm/Classes/Export/MSExcel/*`, `Client/Classes/Import/FirmImporter.cs` | COM на сервере недопустим — лицензия, зависающие процессы, экземпляр на пользователя | подменить реализацию за существующими интерфейсами `IExportDocument` / `IDocumentSheet` на `DocumentFormat.OpenXml` (уже в референсах проекта) |
| OpenOffice UNO: `Export/OOCalc/*`, ссылки `cli_ure`, `cli_uretypes` | то же | удалить ветку, оставить одну OpenXml-реализацию; настройка `ExportOffice` теряет смысл |
| NAudio — прослушивание роликов (`Client/Forms/RollerPassportForm.cs`) | одна форма | `<audio>` плюс эндпоинт стриминга файла |
| Реестр (`RegUserSavePath` = `SOFTWARE\FogSoft\AdvertAg`) | пользовательские настройки в HKCU | таблица настроек в БД |
| Clipboard (`Globals.CopyToClipboard`), печать, локальные пути (`ProposalTemplateFolder` = `Templates\`) | нет прямого аналога | upload/download-эндпоинты, печать средствами браузера или PDF |
| Лицензирование: `License.lic`, `LicenseGenerator`, таймер проверки в `Globals` | модель «на рабочее место» | пересмотреть модель лицензирования |
| `Microsoft.Win32.Registry`, `System.Runtime.Remoting`, `PresentationCore` / `PresentationFramework` | десктопные зависимости | вычистить при выделении ядра |

## 5. Выбор технологии интерфейса

### 5.1. Рекомендация: ASP.NET Core + Blazor Server

Обоснование опирается на свойства именно этого кода, а не на общие соображения:

1. **Весь транспорт данных — `DataSet` / `DataTable` / `DataView`.** Решающий
   аргумент. SPA или подход «Razor плюс JSON-эндпоинты» потребуют
   конвертировать результат каждой из 271 процедуры в DTO — сотни новых классов
   и ровно та работа, которой требование №1 велит избегать. Blazor Server держит
   данные в процессе: `DataTable` биндится напрямую, без сериализации.
2. **`ShowDialog` → `await ShowDialogAsync()`.** Паттерн «открыл модальное окно,
   получил `DialogResult`, продолжил» воспроизводится почти дословно (модалка
   через `TaskCompletionSource`). 56 болезненных мест из раздела 3
   переписываются по одной строке каждое, а не разворачиваются в цепочку
   колбэков и маршрутов.
3. **Состояние экрана.** WinForms держит в памяти граф объектов (`Campaign`,
   `Action`, `DataTable` сетки). Blazor Server держит его так же — компонент
   живёт, пока открыта вкладка. Не нужна ни ре-гидратация состояния из БД на
   каждый запрос, ни дублирование состояния на клиенте.
4. **Метаданные → компоненты.** `PageControl.CreateInstance`, который сейчас по
   XML собирает дерево WinForms-контролов, переносится на `RenderTreeBuilder` —
   та же диспетчеризация по имени элемента.
5. **Один язык, один слой моделей.** Нет webpack, нет npm-бандла, нет
   REST-контрактов между слоями. Для команды с 60 000 строк C# и нулём строк
   фронтенда в этом продукте это и есть простота по критерию №5.

Условия применимости выполняются полностью: внутренняя система, десятки
пользователей, LAN/VPN, одна БД, нет требований мобильности и офлайна. Сейчас
пользователи работают через RDP к серверу — веб убирает этот слой вместе с
RDP-лицензиями и сессиями.

**Минусы, которые надо принять сознательно:**

- каждое действие — round-trip по WebSocket; на плохом канале заметно при вводе
  в сетке (лечится точечно локальным состоянием компонента);
- circuit удерживает память на пользователя (та же память, что сейчас держит
  форма, но на сервере и умноженная на число пользователей);
- нужен корректный сценарий переподключения после обрыва;
- у команды нет опыта Blazor.

### 5.2. Альтернатива: ASP.NET Core MVC + Razor + htmx

Минимум JS, без сборщика. Сложные сетки — по одному изолированному
JS-компоненту с JSON-эндпоинтом. Проще в эксплуатации и ближе к стеку соседнего
продукта компании (iTender: `net8.0`, Razor, jQuery, DataTables, Bootstrap,
webpack). Цена — необходимость DTO-слоя поверх `DataTable` и ручная разборка
диалоговых сценариев.

Выбирать этот вариант, если решено не рисковать с circuit-ами Blazor или если
приоритет — переиспользование опыта команды по iTender.

### 5.3. Отвергнутые варианты

- **SPA на React/Vue плюс REST API.** Максимальный объём работы, два языка,
  полный слой DTO поверх `DataTable`. Прямо противоречит требованию №1.
- **ASP.NET MVC 5 на .NET Framework 4.8.** Соблазнительно — можно сослаться на
  существующие сборки буквально как есть, включая Crystal Reports. Но это
  тупик: платформа в режиме поддержки, только Windows/IIS, второй продукт
  компании уже на .NET 8. Не рекомендуется.

### 5.5. Решение принято: Blazor Server (2026-08-23)

Раздел 5.1 был написан как предварительная рекомендация, до того как стали
известны обстоятельства самой разработки. Когда они выяснились, два пункта
исходного анализа пришлось пересмотреть — оба в пользу Blazor.

**Обстоятельства, которых не было в исходном анализе:**

- миграцию делает **один человек — автор исходной системы**, а не команда;
- его опыт веба: ASP.NET примерно десятилетней давности, **и WebForms, и MVC**;
  современный JS-стек (React и подобное) — нет;
- **сроков нет**, задача делается неспешно и качественно.

**Что это меняет:**

1. **Аргумент за Razor + htmx исчез наполовину.** Раздел 5.2 обосновывал этот
   вариант двумя условиями: «не рисковать с circuit-ами **или** приоритет —
   переиспользование опыта команды по iTender». Второго условия больше нет:
   команда iTender к этой работе отношения не имеет.

2. **«Риск circuit-ов» при честном сравнении разворачивается в плюс.** В
   разделе 5.1 минусами записаны «circuit удерживает память на пользователя» и
   «нужен сценарий переподключения». Сравнивать их надо не с абстрактным
   stateless-веб-приложением, а с тем, что работает **сейчас**: пользователи
   заходят на сервер **по RDP**. То есть постоянное stateful-соединение с
   сервером уже есть; сервер уже держит всё состояние UI, причём целой
   Windows-сессией с процессом `Merlin.exe` на пользователя; надёжность канала
   уже является предусловием работы; обрыв уже переживается переподключением.
   Circuit Blazor — тот же принцип, но на порядок дешевле: граф компонентов
   вместо сессии с процессом. **Память на пользователя не вырастет, а упадёт.**
   По той же причине снимается и минус про плохой канал: по каналу, на котором
   плохо работает Blazor, RDP сегодня работает хуже.

3. **Сетки решают, и решают в пользу C#.** Замер на 2026-08-23: из 19 контролов
   в `Client/Controls` — **14 сетки**, ~7000 строк без Designer, 46 обработчиков
   взаимодействия (клики по ячейкам, drag-drop, редактирование по месту, своя
   отрисовка); `DataGridView` фигурирует в 30 файлах, `SmartGrid` (1911 строк) и
   семейство `TariffGrid` — обёртки вокруг него. Перенос сеток — самый большой
   кусок работы, и он одинаково велик при обоих вариантах. Разница в языке: в
   Blazor это компонент на C# с прямым биндингом `DataTable`; в htmx, по самому
   же разделу 5.2, — «изолированный JS-компонент с JSON-эндпоинтом», то есть
   14 сеток, помноженных на свой JS и свой DTO-слой. Для команды с фронтендером
   это паритет; для одного разработчика без опыта современного JS — нет.

4. **Отсутствие сроков снимает главный минус Blazor.** «У команды нет опыта
   Blazor» из раздела 5.1 — это стоимость обучения, а не технический риск.
   Без дедлайна она приемлема. Важнее оказывается другое: когда помочь некому,
   ценно оставаться в одном языке, который знаешь давно.

**Опыт WebForms работает на нас напрямую.** Blazor Server — это, по существу,
stateful-компоненты с серверными обработчиками событий, то есть WebForms без
ViewState и постбэков; мысленная модель переносится и из WebForms, и из
WinForms. Опыт MVC даст синтаксис Razor, но привычку «request → controller →
view» придётся сознательно отложить: в Blazor состояние живёт между
действиями, и первое время это ломает интуицию.

**Что осталось честной ценой решения** (не отменяется выбором, просто принято):

- готового аналога `DataGridView` в вебе нет — сетки переписываются руками либо
  берётся сторонняя библиотека компонентов (MudBlazor, Radzen); это главный
  объём работы этапа 3;
- отладка обрывов circuit — новый навык (но заменяет собой возню с
  переподключением RDP, а не добавляется к ней);
- Crystal Reports на современном .NET не работает, нужен внешний сервис на .NET
  Framework — но это одинаково при любом выборе фронтенда, см. открытый
  вопрос №4.

### 5.4. Технические решения по инфраструктуре

- **Версия платформы — .NET 10** (выбрано 2026-08-23). На момент выбора это
  текущий LTS: выпущен 11.11.2025, фаза Active, поддержка до 14.11.2028.
  `.NET 8`, на котором изначально писался этот план и был поднят
  `FogSoft.Core`, к августу 2026 уже в фазе Maintenance (только исправления
  безопасности) и выходит из поддержки **10.11.2026** — стартовать на нём
  многолетнюю миграцию значило бы менять рантайм посреди работы. `FogSoft.Core`
  переведён на `net10.0`: сборка прошла без единого нового предупреждения,
  набор замечаний тот же, что был на `net8.0`.
- **SqlClient**: на .NET 10 пакет `System.Data.SqlClient` работает — на первом
  шаге не меняется ни один `using`. Переход на `Microsoft.Data.SqlClient` —
  отдельным шагом позже; учесть, что там `Encrypt=true` по умолчанию, что
  ломает текущие строки подключения.
- **log4net** работает на .NET 10 — формат и файл лога сохраняются
  (`docs/LOGGING.md`).
- **Разделяемые исходники**: чтобы не переносить файлы физически и не ломать
  сборку десктопа, ядро подключается в новый проект через
  `<Compile Include="..\FogSoft.WinForm\DataAccess\*.cs" Link="..." />`.
  Обе UI-платформы живут параллельно всю миграцию.

## 6. Этапы

### Этап 0. Подготовка внутри десктопа (ничего не ломая)

Самый важный этап. Полезен, даже если миграцию отложат или выберут другой
фронтенд.

1. ✅ **Сделано.** Выделить не-UI ядро в новый проект `FogSoft.Core`
   (net10.0): `DataAccess/*`, `SqlHelper`, `Entity` / `EntityManager` /
   `ColumnInfo`, `SecurityManager`, `ConfigurationUtil`, `MessageAccessor`,
   `StringUtil`, не-UI часть `PresentationObject`, не-UI часть `Globals`.
2. ✅ **Сделано.** `SecurityManager.LoggedUser`, `DataAccessor._transaction`,
   `_commandParameters` → на поток выполнения **с сохранением публичного API**.
   Для транзакции — `AsyncLocal`; для пользователя — подставляемое хранилище
   `ILoggedUserStorage` (десктопу остаётся обычное статическое поле, вебу —
   своё, на запрос). Отступление от исходного плана обосновано в коммите:
   прямой `AsyncLocal` на пути входа в систему хрупок из-за `ExecutionContext`.
3. ✅ **Закрыт (2026-08-23) — оказался уже сделанным.** Формулировка «вычистить
   `Application.DoEvents()` и `Cursor.` из разделяемых классов (~103 места,
   механически)» писалась до этапа 0.4 и им же обесценена: 0.4 не удалял эти
   вызовы, а перекладывал в UI-половины — то есть ровно уносил их из
   разделяемого слоя, чего пункт и требовал.

   Замер на 2026-08-23 (557 вхождений всего по репозиторию):

   | Слой | Мест | Что это значит |
   |---|---|---|
   | ядро в мосту (84 файла `Client/Classes/*.cs`) | **0** | цель достигнута |
   | `Launcher.cs`, `MediaPlan.cs` | 3 | сознательно вне моста: точка входа `Main` и отложенный до этапа 4 медиаплан |
   | `Domain/MasterIssue.cs` | 1 | закрыто, коммит `cbadf78` |
   | `GridExport/`, `Import/FirmImporter`, `Exchange/MediaPlus/Export` | 15 | выгрузки/импорт — этап 4 по п. 5 ниже |
   | `Domain/StudioOrder/*` | 9 | мёртвый код, см. ниже |
   | десктоп: `Forms/`, `Controls/`, `*.WinForms.cs` | 536 | в веб не едет, чистить незачем |

   **Чего делать НЕ надо:** сносить `DoEvents`/`Cursor` из 536 десктопных
   мест. Это не подготовка к вебу, а рефакторинг десктопа с реальным риском
   (удаление `DoEvents` меняет поведение при перерисовке и повторном входе)
   и нулевой пользой для миграции. Если такая чистка когда-нибудь понадобится
   — заводить отдельной задачей со своим обоснованием, не под этим пунктом.

   **Попутная находка — `Client/Classes/Domain/StudioOrder/`.** Файлы
   `StudioOrder.cs` и `StudioOrderAction.cs` **никогда не были в
   `Client.csproj`** (проверено по всей истории) и не компилируются, хотя
   метаданные их поднимают рефлексией: сущности 116 и 117 хранят
   `className = Merlin.Classes.Domain.StudioOrder.*` при `assemblyName = Merlin`.
   Обращение к ним упало бы с `TypeLoadException`, но не падает: все 11
   пунктов меню заказов на производство роликов помечены `isObsolete = 1` —
   функция снята с эксплуатации. Разрез к ним применять не стали:
   правки в некомпилируемом коде компилятор не проверяет, а значит
   непроверяемы. Судьба этого кода и метаданных — отдельное решение
   владельца продукта (удалить, вернуть в эксплуатацию или оставить как есть).
4. ✅ **Сделано** (18 партий). Разрезать `ShowDialog` внутри `Client/Classes`
   на «подготовить варианты» и «выполнить с выбором». Конвенция и полный
   отчёт — `docs/tasks/web-migration-dialogs.md`. Осталось только то, что
   сознательно отложено: 4 места формы 3.1 (модальная сессия `CampaignForm`)
   до этапа 3 и `MediaPlan.cs` — до этапа 4.
5. ➡️ **Перенесено в этап 4.** Подменить Excel-экспорт на
   `DocumentFormat.OpenXml` за существующими интерфейсами `IExportDocument` /
   `IDocumentSheet`; то же для `FirmImporter`. Причина переноса: это не
   подготовка десктопа к разрезу, а замена целого слоя выгрузок — она
   тематически принадлежит этапу «Отчёты и выгрузки» и там же должна
   планироваться и проверяться.
6. ❌ **Вычеркнут (2026-08-21).** Предполагалась абстракция чтения
   конфигурации (`app.config` → интерфейс ради `appsettings.json`).
   **Не нужна:** `ConfigurationUtil` уже собирается и работает в
   `FogSoft.Core` на net8 через пакет `System.Configuration.ConfigurationManager`
   — менять в коде нечего. Объём конфигурации — 11 ключей `appSettings` и одна
   живая строка подключения. Шифрование строки подключения своё
   (`ConnectionCryptor`, TripleDES), а не штатный `ProtectedConfigurationProvider`
   .NET Framework, поэтому платформенной несовместимости нет. Достаточно
   положить рядом с веб-приложением файл `<ИмяСборки>.dll.config` (в проекте —
   `App.config`; `web.config` тут ни при чём, он нужен только модулю IIS).
   Вводить интерфейс ради одной реализации — ровно то «улучшение только потому,
   что делаем новое», которого требование №1 велит избегать. Вернуться к
   вопросу, если понадобится разная конфигурация для сред или переменные
   окружения.

**Проверка:** десктоп собирается и работает без функциональных отличий; прогон
смоук-чеклиста из `docs/ARCHITECTURE.md`, раздел «Build, run, and validation».

### Этап 1. Каркас веба

- аутентификация через ту же процедуру `GetUserData` (сделано в срезе);
- контекст пользователя на circuit (сделано в срезе — `WebLoggedUserStorage`,
  на запрос доводится в рамках этапа 1);
- меню из `UserMenuItems` с теми же правами;
- **оболочки с вкладками-аналогом MDI не будет — решено 2026-08-31.**
  Многооконность в вебе даёт браузер бесплатно: вкладка браузера — отдельный
  circuit с независимым состоянием, то есть то же самое, что MDI-child,
  без единой строки своего кода. Своя внутристраничная система окон —
  не перенос на веб, а имитация десктопа внутри веба, ровно то усложнение,
  от которого требования №4-5 (простота, браузер как цель) велят
  отказываться. Навигация — обычная маршрутизация (`/journal/{id}` уже
  так устроен в срезе); сценарий «увидеть второй экран, не теряя первый»
  закрыт модальным диалогом (`DialogService`, проверен в срезе) — настоящих
  случаев двух равноправных долгоживущих окон одновременно в десктопе не
  нашлось, есть только навигация и модальный выбор, оба уже есть;
- log4net в тот же файл и формат (на .NET 10 уже подтверждено срезом,
  `FogSoft.Web/log4net.config`); заполнить `%property{cid}` идентификатором
  запроса (сейчас в паттерне есть, но нигде не присваивается — «Открытые
  вопросы» №3 в `docs/ARCHITECTURE.md`).
  **Найдено 2026-08-31, при обсуждении: десктоп проставляет `%property{user}`
  через `GlobalContext.Properties["user"]` (`MDIForm.cs:104`) — это одно
  значение на весь процесс, в вебе с несколькими пользователями это тот же
  класс бага, что чинили в этапе 0.2 для `SecurityManager.loggedUser` и
  `DataAccessor._transaction`. В вебе `user`/`cid` — через
  `log4net.LogicalThreadContext.Properties` (asynclocal-реализация в
  netstandard2.0-сборке пакета, `System.Runtime.Remoting.CallContext` на
  .NET Core недоступен), проставлять в том же месте circuit, что и
  `WebLoggedUserStorage`. Десктопа не касается — там один пользователь на
  процесс, `GlobalContext` там корректен, менять не нужно.**
- обработка ошибок в семантике `ErrorManager`.

**Проверка:** вход под реальным пользователем, меню совпадает с десктопным по
составу и доступности пунктов.

### Этап 2. Универсальные движки

Журнал, фильтр, паспорт, селекторы, MasterDetail. Закрывает большинство
активных пунктов меню без кода на экран.

**Проверка:** взять 10 разнотипных сущностей (в том числе с зависимыми
lookup-ами, `treeselector`, картинкой) и сверить карточку и журнал с десктопом
поле в поле.

### Этап 3. Специализированные экраны

Порядок по бизнес-ценности:

1. кампания/акция с тарифной сеткой (`CampaignForm`, `TariffGrid`,
   `TariffWithRangeGrid`, `EditIssuesForm`, шаблоны `FrmTemplate*`,
   `FrmGenerator`);
2. калькулятор цен (`PriceCalculatorForm`, `PriceCalculatorGrid`);
3. платежи (`PaymentCandidatesForm`, балансы фирм);
4. журналы с особым поведением (`ActJournalForm`, `AudioJournalForm`,
   `AnnouncementJournalForm`, `StatBalanceJournalForm`);
5. статистика и `GridReport`.

**Проверка:** для каждого экрана — сверка результатов вызовов процедур с
эталоном, снятым до начала работ (раздел 7, п. 7).

### Этап 4. Отчёты и выгрузки

Crystal-сервис, QR (`QRCoder` работает серверно), стриминг аудио роликов, плюс
переехавшая сюда из этапа 0 замена слоя выгрузок:

1. **Excel: COM Interop → `DocumentFormat.OpenXml`** (перенесено из этапа 0
   п.5). Подменить реализацию за существующими интерфейсами `IExportDocument` /
   `IDocumentSheet`; то же для `Client/Classes/Import/FirmImporter.cs`.
   Удалить ветку OpenOffice UNO (`Export/OOCalc/*`, ссылки `cli_ure`,
   `cli_uretypes`) — настройка `ExportOffice` теряет смысл.
2. **`MediaPlan.cs`** — разбирать вместе с этим слоем, а не раньше: файл по
   сути и есть Excel-экспорт. Разбор его текущего состояния (потоков там уже
   нет, вопреки более ранней пометке) — `docs/tasks/web-migration-dialogs.md`,
   §9. Там же зафиксированы два места мёртвого кода в слое экспорта
   (`MSExportDocument.GetNewSheet`, `ExportManager.ExportExcel`), которые
   логично убрать заодно с заменой слоя.

### Этап 5. Перевод в эксплуатацию

Параллельная работа десктопа и веба, переключение по группам пользователей,
затем вывод десктопа из эксплуатации.

## 7. Что обязательно учесть

1. **Права становятся серверным требованием.** Сейчас права гасят пункты меню,
   то есть защита реализована в UI. В вебе эндпоинт вызывается напрямую, минуя
   меню. Проверку прав нужно ставить на каждое действие. Это единственное
   место, где план требует принципиально нового кода, которого в десктопе нет.
2. **Валидация имён сущностей и действий, приходящих с клиента.**
   `DataAccessor.DoAction` резолвит процедуру по ключу
   `EntityId_ActionName_InterfaceObject` из параметров. Клиент не должен иметь
   возможности запросить произвольную сущность или действие. Параметризация
   запросов сама по себе в порядке (`BuildExecScript` — только для
   диагностики).
3. **Долгие операции.** Пересчёт акции и отчёты за широкий период сейчас просто
   вешают UI-поток. В вебе появляются таймауты Kestrel и обратного прокси; нужен
   фоновый запуск с прогрессом хотя бы для отчётов. Кандидаты видны по логу
   `DbExecutionScope` (`StoredProcExecutionTimeThresholdMS`) и по вызовам с
   кастомным command timeout.
4. **Объёмы данных в сетках.** WinForms-сетки показывают тысячи строк без
   постраничности; тарифная сетка — матрица «даты × окна». В HTML нужна
   виртуализация или постраничность, иначе экран умрёт на реальных данных.
5. **Культура и форматы дат.** Культура сейчас ставится на клиенте (`Language`
   = `ru`), даты в интерфейсе — ДД.ММ.ГГГГ. В вебе культура ставится на запрос,
   а HTML-инпуты отдают ISO. Классический источник тихих багов при разборе.
6. **Файловая система.** `Templates\`, файлы роликов, импортируемые выписки —
   всё превращается в серверные пути плюс upload/download-эндпоинты.
7. **Отсутствие тестов.** Тестового проекта в репозитории нет. Переписывать UI
   поверх непокрытой логики рискованно. Минимум перед этапом 3: снять эталонные
   результаты ключевых вызовов процедур на тестовой базе, чтобы сравнивать
   «до и после».
8. **Развёртывание — решено (2026-08-24): Kestrel напрямую, без IIS.**
   На сервере, где сейчас крутится десктоп через RDP, IIS не установлен
   (служба `W3SVC` отсутствует) — заводить его ради обратного прокси
   означало бы новую зависимость сверх того, что нужно. `dotnet publish` +
   Kestrel как служба Windows закрывает автозапуск после перезагрузки без
   IIS. HTTPS и сертификат — если понадобятся при выходе за периметр
   (вопрос №6) — тоже настраиваются в самом Kestrel, IIS для этого не
   обязателен. Вместо раскладки клиента — публикация службой; конвейер
   выката и процедура отката ещё не определены. Текущая автоматизация
   (`AdvertAg.build`, NAnt, `pack.bat`, `PublishUrl`) не переиспользуется.
9. **Многопользовательность на уровне БД не меняется** — десктоп и сейчас
   работает многопользовательски против одной базы, конкурентность
   разруливается процедурами. Новых гонок веб сам по себе не вносит; вносит
   только п. 1.
10. **Память сервера.** Состояние открытых экранов переезжает с рабочих станций
    на сервер. Нужны таймаут простоя, ограничение числа открытых вкладок и
    оценка памяти на пользователя на этапе 2.

## 8. Оценка объёма

Один разработчик, грубый порядок:

| Этап | Недель (оценка до начала) |
|---|---|
| 0. Подготовка ядра | 3–6 |
| 1. Каркас веба | 1–2 |
| 2. Универсальные движки | 5–9 |
| 3. Специализированные экраны | 15–25 |
| 4. Отчёты и выгрузки | 3–5 |
| 5. Перевод в эксплуатацию | 2–4 |
| **Итого** | **29–51** |

Диапазон широкий из-за этапа 3: он зависит от того, насколько точно надо
воспроизвести поведение сеток. После этапа 0 и вертикального среза (раздел 10)
оценка сузится в разы.

**Сверка по факту (2026-08-21):** этап 0 уложился заметно быстрее нижней
границы — основную часть (вынос ~50 диалоговых мест в 20 файлах) закрыли за
18 партий. Помогло то, что чего опасались, не подтвердилось: несовместимостей
с современным .NET в ядре не нашлось вообще ни одной, а один из двух «опасных» файлов
(`MediaPlan.cs`) оказался уже вылеченным. Оценку этапа 4 стоит слегка поднять
— туда переехала замена Excel-слоя из этапа 0. Оценки этапов 1–3 не
пересматривались: там пока нет фактических данных.

## 9. Открытые вопросы

1. ✅ **Закрыт 2026-08-23. Blazor Server** — см. раздел 5.5, где записано,
   что именно изменило решение по сравнению с исходной, предварительной
   рекомендацией.
2. ✅ **Закрыт 2026-08-31. Без пиксельного соответствия.** Та же ошибка
   толкования требования №1, что и MDI-оболочка (см. этап 1, решение
   2026-08-31): требование про минимальные изменения — о бизнес-логике и
   данных, не о поведении интерфейса. План сам уже давал ответ, просто не
   был протянут сюда — раздел 7 п.4: WinForms-сетки показывают тысячи строк
   без постраничности, а «в HTML нужна виртуализация или постраничность,
   иначе экран умрёт на реальных данных». Критерий проверки для этапа 3 —
   не «выглядит и ведёт себя как в десктопе», а «то же самое отдаётся из тех
   же процедур, ничего не потеряно» (та же мера, что и «поле в поле» для
   паспортов на этапе 2 — полнота данных, не подобие интерфейса).
3. Модель лицензирования после перехода на веб.
4. Судьба Crystal Reports в долгую: оставлять внешний сервис на .NET Framework
   бессрочно или планировать замену движка отчётов.
5. Целевое число одновременных пользователей — нужно для оценки памяти сервера
   (п. 10 раздела 7).
6. Нужен ли доступ извне периметра (влияет на аутентификацию и требования к
   каналу, а значит — на минусы из п. 5.1).

## 10. Что сделано и что дальше

**Сделано (этап 0, влит в `master` коммитом `95161fd`).** Первым шагом был
выбран этап 0, а не выбор фреймворка: он приносит пользу независимо от исхода
решения по технологии. Это оправдалось — этап 0 закрыт целиком, а решение по
технологии всё это время работе не мешало и было принято только 2026-08-23,
когда подготовка уже завершилась.

Что получено:

- ядро без WinForms, собирающееся на .NET 10 (`FogSoft.Core`);
- контекст пользователя и транзакция — на поток выполнения, публичное API не
  менялось;
- доменные методы без вшитых диалогов: 22 партии, 59 классов разрезано на
  core/UI-половины (`docs/tasks/web-migration-dialogs.md`);
- **мост закрыт**: `FogSoft.Core` компилирует доменные классы `Client/Classes`
  ссылками и собирается с нулём ошибок — то есть отсутствие UI-зависимостей в
  ядре подтверждено компилятором, а не grep-ом (§10 отчёта по диалогам);
- попутно: убран мёртвый код, починен `.gitignore`, годами скрывавший
  `FogSoft.WinForm/Win32/` от системы контроля версий.

**Технология выбрана** — ASP.NET Core 10 + Blazor Server, раздел 5.5.

### Вертикальный срез — сделан (2026-08-24)

Ветка `feature/web-slice`, 9 коммитов. Сущность 17 «Предмет рекламы» доведена
насквозь: вход → журнал → фильтр → модальный паспорт → сохранение и добавление.
Проверено вживую под реальной учётной записью, все пункты проходят.

#### Замер: сколько стоит один движковый экран

**Ноль строк на экран.** Журнал открывается по адресу `/journal/{id}` для
любой сущности — код на конкретный экран не пишется вообще. Главная ставка
плана из раздела 2 подтверждена практикой, а не рассуждением.

Написано всего 966 строк, и это вместе с комментариями: в файлах
инфраструктуры 175 строк из 404 — пояснения к принятым решениям, оставленные
намеренно. Из этих 966 к «движкам» относится около 520 (журнал, паспорт,
разбор XML, диалоги), остальное — разовая обвязка приложения: вход, контекст
пользователя, загрузка справочников, сопоставление сборки.

**Покрытие на сегодня.** Активных сущностей с паспортом — 56. Из них **34
(61%) используют только `<field>`**, то есть получают работающий экран уже
сейчас, без единой строки. Остальные ждут контролов, которых в движке пока
нет:

| Контрол | Сущностей ждёт |
|---|---|
| `lookup` | 14 |
| `objectPicker` | 11 |
| `selector` | 4 |
| `button` | 2 |
| `image` | 2 |
| `treeselector` | 0 |

Список короткий и конечный — это и есть содержание работы по паспортам в
этапе 2, теперь измеренное, а не оценённое на глаз.

#### Что подтвердилось

- **Ядро переиспользуется целиком.** Своего SQL в вебе нет ни строки: журнал
  идёт через `Entity.GetContent`, сохранение — через `PresentationObject.Update()`,
  то есть те же IUD-процедуры. Мост этапа 0.4 окупился ровно здесь.
- **Довод 5.1 №2 про диалоги — верен буквально.**
  `if (form.ShowDialog(owner) == DialogResult.OK)` превратился в
  `if (await Dialogs.ShowAsync(...) == DialogOutcome.Ok)`. Механика —
  `TaskCompletionSource`; работает потому, что состояние компонента в Blazor
  Server живёт между действиями, как жил бы стек WinForms-формы. Это был самый
  весомый аргумент за выбранную технологию, и он выдержал проверку.
- **Шов `ILoggedUserStorage` из этапа 0.2 подошёл без правок ядра.**

#### Находки, видимые только на живом прогоне

Ни одна из них не видна в архитектуре — ради них срез и делался:

1. **`DataAccessor.procedureConfigs` не грузится лениво.** Все остальные
   словари ядра догружаются сами при промахе кэша, этот — нет: `DoAction`
   берёт `procedureConfigs[key]` напрямую и падает с `KeyNotFoundException`.
   В десктопе его грузит `SplashLogginForm` после входа; в вебе понадобился
   свой вызов (`CoreBootstrap`).
2. **Метаданные жёстко называют сборку десктопа.** `iEntity.assemblyName` для
   доменных сущностей — `Merlin`, то есть `Merlin.exe`. В вебе те же классы
   лежат в `FogSoft.Core.dll`, и `Entity.CreateObject` их не находил. Решено
   сопоставлением имён на стороне веба (`DomainAssemblyResolver`); менять
   метаданные нельзя — база общая с десктопом.
3. **Смешение статической и интерактивной отрисовки раздваивает
   scoped-сервисы.** Шаблон Blazor делает страницы статическими по умолчанию;
   макет оставался статическим и получал *другой экземпляр* scoped-сервиса,
   чем интерактивная страница. Симптом (диалог «уже открыт», но не виден)
   совершенно не похож на причину. Лечится объявлением интерактивности
   глобально — для замены десктопа статическая отрисовка не нужна нигде.
4. **Фильтр может быть мёртвыми метаданными.** У сущности 17 фильтр объявляет
   поле `isHidden`, которого процедура `AdvertTypes` не принимает вовсе;
   параметр молча отбрасывается при сверке с реальными параметрами процедуры.
   Так же ведёт себя и десктоп — это не дефект веба. Важно для этапа 2:
   «фильтр не работает» может означать заброшенную функцию, а опечатка в имени
   параметра фильтра никак себя не проявит.

#### Сознательно не сделано

- **Древовидный журнал.** У сущности 17 в десктопе иерархия
  (`AdvertType.GetContent` по `parentID`); в срезе журнал плоский. Дерево —
  отдельный движок этапа 2, а не разовая доработка экрана.
- **Права на действия.** Раздел 7 п. 1: в вебе проверку прав нужно ставить на
  каждое действие, а не гасить пункты меню. В срезе этого нет — это работа
  этапа 1 и единственное место, где план требует принципиально нового кода.
- **Всё остальное из этапа 1**: меню из `UserMenuItems`, `%property{cid}` в
  логе, обработка ошибок в семантике `ErrorManager`. Оболочка с вкладками —
  не отложена, а отменена, см. выше.

### Что дальше

Замер получен, оценка этапа 3 может опираться на факты. Следующий шаг —
собственно **этап 1** (каркас: меню, права, вкладки, логирование запроса),
после него **этап 2** — недостающие контролы паспорта по таблице выше и
древовидный журнал.
